[package]
name = "chronos-core"
version = "0.1.0"
edition = "2021"

[features]
default = []
ebpf = ["dep:aya"]

[dependencies]
tokio.workspace = true
anyhow.workspace = true
hex.workspace = true

[target.'cfg(unix)'.dependencies]
libc = "0.2"
aya = { version = "0.3.1", optional = true }
